## 2024-05-22 - Semantic Cards
**Learning:** Large interactive cards with complex content can be implemented as semantic `<button>` elements using `text-left` and `w-full` to preserve layout while gaining native keyboard accessibility.
**Action:** Replace `div` with `onClick` with `<button type="button">` for card-based navigation.
