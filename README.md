# Kilo a Ko'a

Community-based coral reef monitoring and conservation application.

## Getting Started

1. Install dependencies:
   ```bash
   npm install
   ```

2. Start the development server:
   ```bash
   npm run dev
   ```

## Testing

This project uses [Playwright](https://playwright.dev/) for end-to-end testing of different user roles (Donor, Scientist, Admin).

To run the tests:

```bash
npx playwright test
```

To view the test report:

```bash
npx playwright show-report
```
